# E4: what the ortholattice normaliser is worth as a preprocessing step, on and off, everything else equal.
defaults dataset=casc-j13-fof mode=cert strategy=balanced timeout=180000

orthologic-off  orthologic=off
orthologic-on   orthologic=on
